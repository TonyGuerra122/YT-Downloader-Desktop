namespace YTDownloaderDesktop.Environment;

public static class Constants
{
    public static readonly string DownloadsFolder = System.Environment.GetFolderPath(System.Environment.SpecialFolder.UserProfile) + "\\Downloads";
}
